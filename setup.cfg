[flake8]
max-line-length = 88
extend-ignore = E203, W503
exclude =
    .git,
    __pycache__,
    .venv,
    venv,
    migrations,
    docs

[tool:pytest]
testpaths = tests
addopts = --cov=app --cov-report=term-missing -v
pythonpath = .
